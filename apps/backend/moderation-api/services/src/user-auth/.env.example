PORT=3000
DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/restaurant_reviews
SESSION_SECRET=change_this_to_something_random

# Root moderator bootstrap (used by the moderation service via the /json API)
# The account is created automatically on server start if it does not exist.
ROOT_MODERATOR_NAME=Root Moderator
ROOT_MODERATOR_EMAIL=moderator@admin.com
ROOT_MODERATOR_PASSWORD=moderator123
