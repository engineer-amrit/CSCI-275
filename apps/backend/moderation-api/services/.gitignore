# Ignore cloned service directories
# Only keep the configuration files and scripts

# Service directories (uncomment when services are cloned)
user-auth/
restaurant/
review/
media/
search/

# Keep configuration files
!config.json
!setup.sh
!README.md
!docker-compose.yml
!Dockerfile.template

# Ignore node_modules in services
*/node_modules/

# Ignore build artifacts
*/dist/
*/build/

# Ignore environment files
*/.env
*/.env.local
*/.env.development
*/.env.production
